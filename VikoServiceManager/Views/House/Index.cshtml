@model IEnumerable<House>
<br />
<div class="row">
    <div class="col-6">
        <h2 class="text-primary">House List</h2>
    </div>
    <div class="col-6">
        <a asp-action="Upsert" asp-asp-controller="House" class="btn btn-primary float-end">Create New House Entry</a>
    </div>
</div>
<div class="p-4 border rounded">
    @if (Model.Any())
    {
        <table class="table table-striped border">
            <tr class="table-secondary">
                <th>
                    Id
                </th>
                <th>
                    Address
                </th>
                <th>
                    City
                </th>
                <th>
                    Region
                </th>
                <th>
                    PostalCode
                </th>
                <th>
                    Resident Group
                </th>
                <th>
                    Service Name
                </th>
            </tr>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @item.Id
                    </td>
                    <td>
                        @item.Address
                    </td>
                    <td>
                        @item.City
                    </td>
                    <td>
                        @item.Region
                    </td>
                    <td>
                        @item.PostalCode
                    </td>
                    <td>
                        @item.ResidentGroupName
                    </td>
                    <td>
                        @item.ServiceName
                    </td>
                    @*                    <th>
            <div class="text-center">
            <form asp-action="Delete" asp-route-id="@item.Id">
            <a asp-action="Upsert" asp-route-id="@item.Id" class="btn btn-info text-white" style="cursor:pointer">
            Edit
            </a>

            <button type="submit" onclick="return confirm('Are you sure you want to delete this?')" class="btn btn-danger">
            Delete
            </button>
            </form>
            </div>
            </th>*@
                </tr>
            }
        </table>
    }
</div>