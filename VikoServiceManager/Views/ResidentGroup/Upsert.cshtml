@model ResidentGroupViewModel

@{
    var title = "Create Resident Group";
}

<form method="post">
    <input asp-for="@Model.Id" hidden />
    @if (Model != null && !String.IsNullOrEmpty(Convert.ToString(Model.Id)))
    {
        title = "Edit Create Resident Group";
    }

    <div class="border p-3 bg-white">
        <div class="form-group row">
            <h2 class="text-info pl-3">@title</h2>
        </div>
        <div class="row">
            <div class="col-8">
                <div class="form-group row py-2">
                    <div class="col-4">
                        <label asp-for="Id"></label>
                    </div>
                    <div class="col-8">
                        <input asp-for="Id" readonly class="form-control"></input>
                    </div>

                    <div class="col-4">
                        <label asp-for="Name"></label>
                    </div>
                    <div class="col-8">
                        <input asp-for="Name" class="form-control"></input>
                        <span asp-validation-for="Name" class="text-danger"></span>
                    </div>
                    <div class="col-4">
                        <label asp-for="Description"></label>
                    </div>
                    <div class="col-8">
                        <input asp-for="Description" class="form-control"></input>
                        <span asp-validation-for="Description" class="text-danger"></span>
                    </div>
                    <div class="form-group row py-2">
                        <div class="col-8 offset-4 row">
                            <div class="col">
                                @if (Model != null && !String.IsNullOrEmpty(Convert.ToString(Model.Id)))
                                {
                                //update
                                    <input type="submit" class="btn btn-primary w-100" value="Update" />
                                }
                                else
                                {
                                    // create
                                    <input type="submit" class="btn btn-primary w-100" value="Create" />
                                }
                            </div>
                            <div class="col">
                                <a asp-action="Index" class="btn btn-success w-100">Back</a>
                            </div>
                        </div>
                    </div>

                </div>
            </div>
        </div>
</form>